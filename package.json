{
  "name": "codex-mem",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "sanity": "node dist/server.js --sanity",
    "test": "node dist/server.js --tests"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3"
  }
}
